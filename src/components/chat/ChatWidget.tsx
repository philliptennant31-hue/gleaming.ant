/**
 * Chat widget — "Sparkle, the Gleaming Ant assistant".
 *
 * Phase 1 stub: renders nothing. Phase 4 replaces the contents of this file
 * with the floating amber button + panel (see docs/SPEC.md, Chatbot). It is
 * already mounted in Layout so Phase 4 only needs to fill this component in.
 */
export default function ChatWidget() {
  return null
}
